[dev-packages]
mypy = "*"
pylama = ">=7.0.0"
bandit = ">=1.0.1"
pytest = ">=6.0.0"
coverage = ">=5.0"
sphinx-autodoc-typehints = ">=1.0.0"
pre-commit = ">=2.0.0"
black = ">=20.8b0"
isort = ">=5.0.0"

[pipenv]
allow_prereleases = true
