import * as QUnit from "qunit";
import { default as Parser } from "../src/rosetta-lang/parser";


QUnit.module('parser');


QUnit.test('add two numbers', assert => {
    let instance = new Parser("");
});
